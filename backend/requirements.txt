# Backend dependencies for Patient 360
# Python 3.11+

# Web framework
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6

# Database - using pg8000 (pure Python, no compilation required)
pg8000>=1.30.0

# Data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Azure services (optional, for direct SDK calls if needed)
azure-ai-textanalytics>=5.3.0
openai>=1.12.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.26.0
